---
# ===================================================================
# 1. Config utilisateur libvirt
# ===================================================================

- name: 1.1 Créer ~/.config/libvirt
  ansible.builtin.file:
    path: "{{ ansible_facts.env.HOME }}/.config/libvirt"
    state: directory
    mode: '0755'

- name: 1.2 Configurer virsh (qemu:///system)
  ansible.builtin.copy:
    dest: "{{ ansible_facts.env.HOME }}/.config/libvirt/libvirt.conf"
    content: 'uri_default = "qemu:///system"'
    mode: '0644'

# ===================================================================
# 2. Réseau libvirt : détection, correction, création
# ===================================================================

# --- 2.1 Détecter si vagrant-libvirt existe (il utilise virbr0)
- name: 2.1 Vérifier si le réseau vagrant-libvirt existe
  become: true
  ansible.builtin.command: virsh net-info vagrant-libvirt
  register: vagrant_net
  failed_when: false
  changed_when: false

- name: 2.2 Définir si virbr0 est réservé par vagrant-libvirt
  ansible.builtin.set_fact:
    virbr0_reserved: "{{ vagrant_net.rc == 0 }}"

# --- 2.3 Corriger le DHCP du réseau default (uniquement si virbr0 est libre)
- name: 2.3 Corriger le DHCP du réseau default
  become: true
  ansible.builtin.replace:
    path: /usr/share/libvirt/networks/default.xml
    regexp: '<range start="192\.168\.121\.1" end="192\.168\.121\.254"/>'
    replace: '<range start="192.168.121.2" end="192.168.121.254"/>'
  when: not virbr0_reserved

# --- 2.4 Vérifier si le réseau default existe
- name: 2.4 Vérifier si le réseau default est listé
  become: true
  ansible.builtin.command: virsh net-list --all
  register: net_list
  changed_when: false

- name: 2.5 Définir si le réseau default existe
  ansible.builtin.set_fact:
    libvirt_default_exists: "{{ 'default' in net_list.stdout }}"

# --- 2.6 Créer le réseau default si possible
- name: 2.6 Créer réseau default si absent et virbr0 libre
  become: true
  ansible.builtin.command: virsh net-define /usr/share/libvirt/networks/default.xml
  when: not libvirt_default_exists and not virbr0_reserved

# --- 2.7 Vérifier état du réseau default
- name: 2.7 Vérifier si le réseau default est actif
  become: true
  ansible.builtin.command: virsh net-info default
  register: net_info
  failed_when: false
  changed_when: false
  when: not virbr0_reserved

# --- 2.8 Démarrer le réseau default si inactif
- name: 2.8 Démarrer réseau default si inactif
  become: true
  ansible.builtin.command: virsh net-start default
  when:
    - not virbr0_reserved
    - "'inactive' in net_info.stdout"
  register: net_start
  failed_when: false
  changed_when: net_start.rc == 0

# --- 2.9 Activer autostart
- name: 2.9 Activer autostart réseau default
  become: true
  ansible.builtin.command: virsh net-autostart default
  when: not virbr0_reserved
  failed_when: false
  changed_when: false

# ===================================================================
# 3. firewalld : NAT + masquerade + source
# ===================================================================

- name: 3.1 Ajouter masquerade dans la zone libvirt
  become: true
  ansible.builtin.command: firewall-cmd --zone=libvirt --add-masquerade --permanent
  register: fw_masq
  failed_when: false
  changed_when: fw_masq.rc == 0

- name: 3.2 Ajouter forward dans la zone libvirt
  become: true
  ansible.builtin.command: firewall-cmd --zone=libvirt --add-forward --permanent
  register: fw_fwd
  failed_when: false
  changed_when: fw_fwd.rc == 0

- name: 3.3 Ajouter la source 192.168.121.0/24 dans la zone libvirt
  become: true
  ansible.builtin.command: firewall-cmd --zone=libvirt --add-source=192.168.121.0/24 --permanent
  register: fw_src
  failed_when: false
  changed_when: fw_src.rc == 0

# ===================================================================
# 4. Règles directes pour contourner Docker
# ===================================================================

- name: 4.1 Autoriser FORWARD -i virbr0
  become: true
  ansible.builtin.command: firewall-cmd --permanent --direct --add-rule ipv4 filter FORWARD 0 -i virbr0 -j ACCEPT
  register: fw_dir1
  failed_when: false
  changed_when: fw_dir1.rc == 0

- name: 4.2 Autoriser FORWARD -o virbr0
  become: true
  ansible.builtin.command: firewall-cmd --permanent --direct --add-rule ipv4 filter FORWARD 0 -o virbr0 -j ACCEPT
  register: fw_dir2
  failed_when: false
  changed_when: fw_dir2.rc == 0

# ===================================================================
# 5. Détection interface Internet (Wi‑Fi ou Ethernet)
# ===================================================================

- name: 5.1 Détecter interface Internet active
  ansible.builtin.command: nmcli -t -f DEVICE,STATE d
  register: nmcli_devices

- name: 5.2 Extraire interface active
  ansible.builtin.set_fact:
    internet_iface: "{{ nmcli_devices.stdout_lines | select('search', ':connected') | map('regex_replace', ':connected', '') | list | first }}"

- name: 5.3 Ajouter interface Internet dans zone public
  become: true
  ansible.builtin.command: firewall-cmd --zone=public --add-interface={{ internet_iface }} --permanent
  register: fw_iface
  failed_when: false
  changed_when: fw_iface.rc == 0

# ===================================================================
# 6. Activer ip_forward
# ===================================================================

- name: 6.1 Activer net.ipv4.ip_forward
  become: true
  ansible.builtin.copy:
    dest: /etc/sysctl.d/99-ipforward.conf
    content: "net.ipv4.ip_forward = 1\n"
    mode: '0644'

# ===================================================================
# 7. Désactiver rp_filter (Wi‑Fi + virbr0)
# ===================================================================

- name: 7.1 Désactiver rp_filter (all, interface Internet, virbr0)
  become: true
  ansible.builtin.copy:
    dest: /etc/sysctl.d/99-rpfilter.conf
    content: |
      net.ipv4.conf.all.rp_filter = 0
      net.ipv4.conf.{{ internet_iface }}.rp_filter = 0
      net.ipv4.conf.virbr0.rp_filter = 0
    mode: '0644'

# ===================================================================
# 8. Recharger sysctl + firewalld
# ===================================================================

- name: 8.1 Recharger sysctl
  become: true
  ansible.builtin.command: sysctl --system

- name: 8.2 Recharger firewalld
  become: true
  ansible.builtin.command: firewall-cmd --reload

# ===================================================================
# 9. Installer plugins vagrant-libvirt + vagrant-mutate
# ===================================================================

- name: 9.1 Installer plugin vagrant-libvirt
  ansible.builtin.command: vagrant plugin install vagrant-libvirt
  register: vagrant_plugin_install
  changed_when: "'installed' in vagrant_plugin_install.stdout"

- name: 9.2 Installer plugin vagrant-mutate
  ansible.builtin.command: vagrant plugin install vagrant-mutate
  register: vagrant_mutate_install
  changed_when: "'installed' in vagrant_mutate_install.stdout"
